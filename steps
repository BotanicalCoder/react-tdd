1. install jest, cypress and react testing library
2. setup cypress ( create the cypress folder inside the base directory, create the e2e folder inside the cypress folder, create the support folder inside the e2e folder, create the index.cy.ts file inside the e2e folder )
3. create folders inside of the cypress folder, 1 for each test category; e2e, integration, unit